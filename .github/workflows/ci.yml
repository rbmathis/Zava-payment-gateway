name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Building Zava-payment-gateway..."
      - name: Test
        run: echo "Testing Zava-payment-gateway... All tests passed."
